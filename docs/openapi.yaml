openapi: "3.0.0"
info:
  title: EFV ICBC API
  version: "0.1.1"
  description: EFV ICBC API
servers:
  - url: http://localhost:8080
  - url: https://efv-icbc-api-17db4f-dev.apps.silver.devops.gov.bc.ca
    description: Development
paths:
  /health:
    get:
      summary: Health check
      tags: [health]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
