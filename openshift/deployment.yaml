apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-sample-api
  labels:
    app: go-sample-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-sample-api
  template:
    metadata:
      labels:
        app: go-sample-api
    spec:
      containers:
        - name: go-sample-api
          image: REPLACE_WITH_IMAGE
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
