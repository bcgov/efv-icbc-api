apiVersion: v1
kind: Service
metadata:
  name: efv-icbc-api
  labels:
    app: efv-icbc-api
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: efv-icbc-api
